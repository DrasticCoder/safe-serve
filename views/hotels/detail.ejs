<!DOCTYPE html>
<html>
<head>
  <title>Hotel Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #fddecc;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    .container {
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 90%;
      max-width: 800px;
      margin-top: 20px;
      box-sizing: border-box;
    }
    h1 {
      color: #333;
      margin-bottom: 15px;
      font-size: 24px; /* Adjust font size */
    }
    p {
      color: #555;
      font-size: 18px; /* Adjust font size */
      margin: 5px 0;
    }
    img {
      max-width: 100%;
      height: auto;
      border: 2px solid #ddd;
      border-radius: 8px;
      margin: 15px 0;
    }
    button {
      padding: 10px 20px;
      font-size: 16px; /* Adjust font size */
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-top: 20px;
    }
    .button-primary {
      background-color: #ff9900;
      color: #fff;
    }
    .button-secondary {
      background-color: #f4f4f4;
      color: #000;
    }
    form {
      margin-top: 20px;
    }

    /* Responsive styles */
    @media (max-width: 600px) {
      h1 {
        font-size: 20px;
      }
      p {
        font-size: 16px;
      }
      button {
        padding: 8px 16px;
        font-size: 14px;
      }
      .container {
        padding: 15px;
      }
      img {
        margin: 10px 0;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= hotel.name %></h1>
    <p><strong>Rating Range:</strong> <%= hotel.ratingRange %></p>
    <p><strong>Location:</strong> <%= hotel.location %></p>
    <p><strong>Visitors:</strong> <%= hotel.visitorCount %></p>
    <img id="qrCodeImage" src="<%= hotel.qrLink %>" alt="QR Code" />
    
    <form action="/hotels/generate-qr/<%= hotel._id %>" method="post">
      <button type="submit" class="button-secondary">Generate QR</button>
      <button id="downloadQR" type="button" class="button-primary">Download QR Code</button>
    </form>
  </div>

  <script>
    document.getElementById('downloadQR').addEventListener('click', function() {
      var qrImage = document.getElementById('qrCodeImage');
      var link = document.createElement('a');
      link.href = qrImage.src;
      link.download = 'hotel-qr-code.png'; // Name of the file to be downloaded
      link.click();
    });
  </script>
</body>
</html>
